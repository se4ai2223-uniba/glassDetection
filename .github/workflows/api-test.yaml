name: api_testing_workflow

on:
  push:
    paths:
      - 'app/**'

jobs:
  api-testing:
    name: API testing
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
      - name: Set up Python 3.9
        uses: actions/setup-python@v2
        with:
          python-version: 3.9
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r ./requirements.txt
          pip install httpx
          pip install dvc
          pip install dvc-gdrive
      - name: download files from DVC
        env: 
            #GDRIVE_CREDENTIALS_DATA: ${{ secrets.GDRIVE_CREDENTIALS_DATA }}
            GDRIVE_CLIENTID: ${{ secrets.GDRIVE_CLIENTID }}
            GDRIVE_CLIENT_SECRET: ${{ secrets.GDRIVE_CLIENT_SECRET }}
        run: |
          dvc remote modify remote_datasets gdrive_client_id $GDRIVE_CLIENTID
          dvc remote modify remote_datasets gdrive_client_secret $GDRIVE_CLIENT_SECRET
          dvc pull models/finalModelGlassDetection255.dvc
      - name: Test the API with pytest
        run: |
          pytest app/
