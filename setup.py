from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Deploy of a ML (CNN - based) model able to detect subjects wearing glass in photos.',
    author='Glass Detection Team',
    license='MIT',
)
